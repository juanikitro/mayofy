# data-verifier

Verifica existencia, direccion, telefono, horarios, calificacion, resenas, fotos y ausencia de sitio web propio.

No completa huecos. Si no puede verificar un dato, lo marca como `null`, agrega `missing_data_reason` y bloquea `approved_for_generation`.
